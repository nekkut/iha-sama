{
  "name": "softwareassignment",
  "version": "1.0.0",
  "description": "testi",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "coverage": "npm run test && jest --coverage && coveralls < coverage/lcov.info "
  },
  "author": "ville",
  "license": "MIT",
  "dependencies": {
    "jest": "^26.6.3",
    "jest-extended": "^0.11.5",
    "lint": "^0.7.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.0"
  }
}
